# AI providers — install at least one
anthropic>=0.34.0
openai>=1.30.0
google-genai>=1.0.0

# HTTP (urllib3 still used internally by other packages)
urllib3>=2.0.0

# Note: requests and beautifulsoup4 are no longer needed.
# All HTTP scanning is handled by the Go scanner binary.
# All DOM parsing is handled by the TypeScript/Playwright crawler.
